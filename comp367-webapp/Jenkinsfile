pipeline {
    agent any

    tools {
        maven 'MAVEN3'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Maven') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'mvn -f comp367-webapp/pom.xml clean install'
                    } else {
                        bat 'mvn -f comp367-webapp/pom.xml clean install'
                    }
                }
            }
        }
    }

    post {
        success {
            echo 'Build successful!'
        }
    }
}
